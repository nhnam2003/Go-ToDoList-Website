 go mod init github.com/DaiNef163/Go-ToDoList
 go get github.com/gofiber/fiber/v2
 go install github.com/air-verse/air@latest
 go get github.com/joho/godotenv


 myapp/
│
├── cmd/
│   └── main.go            # Entry point, nơi chạy ứng dụng
│
├── internal/
│   ├── todo/              # Package xử lý todo
│   │   ├── domain/        # Các mô hình domain (Entities)
│   │   │   └── todo.go    # Mô hình ToDo
│   │   ├── usecase/       # Logic nghiệp vụ (Use cases)
│   │   │   ├── create.go  # Create ToDo use case
│   │   │   ├── update.go  # Update ToDo use case
│   │   │   └── delete.go  # Delete ToDo use case
│   │   ├── handler/       # Các HTTP handler
│   │   │   └── todo.go    # Xử lý HTTP request
│   │   └── repository/    # Giao tiếp với MongoDB
│   │       └── todo.go    # CRUD operations với MongoDB
│   ├── infrastructure/    # Các adapter khác (ví dụ: MongoDB connection)
│   └── config/            # Cấu hình chung cho ứng dụng
└── go.mod                 # Module definition
